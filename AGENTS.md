# AGENTS.md

## 项目工作规则

- 默认中文沟通，称呼用户为 Sir。
- 先读现有代码结构和配置，再做修改；优先沿用本项目已有写法。
- 修改范围保持克制，不顺手重构无关代码。
- 工作区已有改动默认视为用户改动，不回滚、不覆盖，除非用户明确要求。

## 授权与范围

- 沿用全局自主执行与授权原则。用户已要求实施且范围明确时，必要的代码、局部配置、路径和文件修改直接完成，不再要求逐项计划批准；只审计或只出方案时不修改。
- 不将发现的路径、配置占位符或潜在改进当作授权。普通实现细节可自行决定；涉及真实数据去向、账号、启用监控源或真实归档时，先核对目标与已有授权，仅对未获准部分询问。
- Obsidian / LLM_WIKI / raw / GoogleDrive 等敏感重要目录按任务需要使用。写入、删除、批量移动或改配置指向须有覆盖具体对象与动作的授权；不因目录存在自行操作，也不重复索取已有批准。
- 不引用其他产品的全局指令作为 Codex 的优先级规则；按当前平台的指令层级、作用范围和用户最新要求处理冲突。

## 验证规则

- 本项目不要启动 Streamlit 做页面验证；不要运行 `streamlit run`，也不要为了截图或预览去打开本地 Streamlit 服务。
- 改完代码后优先使用以下方式验证：
  - 相关单元测试，例如 `python -m unittest tests.test_web_memo_db`
  - 语法检查，例如 `python -m py_compile <file>`
  - 纯函数输出检查，例如直接测试 HTML/Markdown/Excel 生成函数
- 如果页面视觉需要确认，只说明已做代码级验证和剩余视觉风险，不再尝试本地启动 Streamlit。
- 全量测试如果因既有依赖问题失败，要说明失败模块和原因，不把无关失败当作本次改动失败。

## UI 偏好

- 卡片式界面要紧凑、信息密度高，不要做成很高的大卡片；一个屏幕只能看到一两个卡片的布局不符合本项目偏好。
- 记录类卡片优先采用横向布局：文件名、状态和基础信息放左侧，备注/分类标记和保存动作放右侧，详细内容放在下方展开区。
- 状态标签尽量贴近标题或文件名，不要单独占用大块区域。
- 能用“展开查看”承载长文本时，不要默认把原文、整理稿、长备注全部展开占满页面。

## 更新日志规则

- 文档正式命名：中文版 `README_ZH-CN.md` / `CHANGELOG_ZH-CN.md`，英文版 `README_EN.md` / `CHANGELOG_EN.md`；`README.md` 和 `CHANGELOG.md` 保留为 GitHub 默认入口，镜像英文主版本并链接中文版。
- 默认不再每次代码修改都同步更新 README 或 Change Log，避免小改动反复消耗大量 token。
- 小改动、按钮文案、局部 UI 微调、测试补充、内部实现修复，默认只做代码和测试，不更新文档。
- 用户明确要求“对齐文档 / 更新 README / 更新 Change Log / 汇总最近几次改动”时，再集中维护中英文版本。
- 也可以阶段性对齐：例如累计 5 次左右代码提交后，再通过 `git log`、`git show`、`git diff <range>` 汇总这段时间的改动，一次性更新 README / Change Log。
- 如果单次改动涉及新功能、使用方式、数据同步规则、页面说明、公开入口或用户必须知道的行为变化，才主动建议同步更新文档；是否立即更新由用户确认。
- Change Log 记录日期、最终用户变化、兼容性、重要修复和已知限制；不逐次记录普通调试、失败测试和绕路。
- 只有影响后续维护判断的失败原因与恢复过程才记录到 docs/history，并保留必要证据。不夸张，不把未验证效果写成已完成。

## Git 规则

- 用户已授权：完成代码修改、验证通过、检查差异没问题后，自动提交并 `git push` 当前分支。
- 以下操作需要覆盖具体对象和动作的授权，已有授权不重复询问：删除文件或目录、修改密钥/`.env`/账号凭据、重置或改写 Git 历史、强制推送、部署发布、安装全局工具、修改外部账号权限、**改写指向敏感目录的路径配置**、**在 Obsidian/LLM_WIKI/raw/GoogleDrive 等敏感位置写入或删除**、**未确认就启用监控源或真归档**。

## 线上线下数据同步规则

- GitHub 远端仓库视为动态数据的最新账本；线上 Streamlit 通过 GitHub token 写入的数据，和本地任务生成的数据，必须先在远端汇合后再继续写入。
- 修改 `data/` 动态文件前先 `git fetch origin` 并比较分歧。远端仅领先且工作区干净时使用 `git pull --ff-only origin main`；存在本地未推送提交时，先检查差异，必要时在干净工作区用 `git pull --rebase origin main` 整合。不得自动 stash、覆盖未提交修改或改写已发布历史。推送前再检查远端变化并处理真实冲突。
- 动态数据文件重点包括：`data/budget_ledger_backup.md`、`data/web_memos_backup.md`、`data/ding_minutes_cloud.json`，以及后续新增的线上可写备份文件。
- 禁止跳过远端同步直接覆盖动态数据文件；禁止为了推送成功而使用强制推送。强制推送、重置历史、丢弃远端数据都必须先单独询问用户。
- 如果 GitHub Contents API 写入遇到 409 冲突或远端文件已更新，默认保留远端优先，不静默覆盖；应提示先同步远端，再合并本地数据后重试。
- 本地自动任务的推荐顺序是：先拉取远端最新数据，再扫描/生成本地数据，再检查差异，再提交，再推送。
- 处理代码改动和数据改动时尽量分开提交；代码提交不要顺手覆盖动态数据，数据同步提交也不要夹带无关代码修改。

## 数据和备份

- 不把密钥、token、密码写入代码或提交。
- 预算台账数据相关改动要特别注意备份链路；不要把测试数据写进真实 `data/` 备份文件。
- 涉及结构化数据时优先使用 SQLite、Excel/JSON/Markdown 等结构化读写方式，不用脆弱字符串拼接硬凑。
